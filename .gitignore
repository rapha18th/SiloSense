data/spid_downloads/
data/esc50/
data/*.log
*.pyc
__pycache__/
.ipynb_checkpoints/
*.zip
kaggle.json

# Regeneratable pipeline outputs (deterministic given a fixed RNG seed and
# the tracked aspids_log.csv). aspids_log.csv itself is NOT ignored — it's
# an external label log nothing here regenerates.
data/manifest.csv
data/spid_selected.csv
data/spid_candidates.csv

# Android build/cache output
android/.gradle/
android/.idea/
android/.kotlin/
android/local.properties
android/app/build/
android/build/
android/*.iml
android/app/*.iml
android/captures/
android/.cxx/
*.log

# Superseded Termux/proot prototype and the submission writeup generator —
# neither is code the app ships, kept on disk locally, not tracked.
legacy_termux_proot/
submission/

# Session-handoff/narrative documents, not code — see NEXT_STEPS.md itself
# for why these stay local-only.
*.docx
~$*.docx
*.pdf
NEXT_STEPS.md
PORTING_NOTES.md
DEVPOST_SUBMISSION.md

# Logo/wordmark source, not app code.
branding/

# Claude Code session state, not project content.
.claude/
